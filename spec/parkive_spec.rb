# frozen_string_literal: true

RSpec.describe Parkive do
  it "has a version number" do
    expect(Parkive::VERSION).not_to be nil
  end

  xit "does something useful" do
    expect(false).to eq(true)
  end
end
